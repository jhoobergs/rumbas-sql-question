---
type: template
template: db2
statement_nl: Bepaal het aantal bestellingen in een kolom {amount}.
statement_en: Determine the amount of orders in a column {amount}.
answer: |-
  "SELECT count(*) as {amount} FROM \"{Order}\";"
